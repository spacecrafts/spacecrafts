package se.jbee.game.scs.screen;

public class SavingGame {

	// screen der nur sagt, dass gespeichert wird 
	// screen wird auf returnscreen gesetzt, returnscreen entfernt (in show)
	// keine input mappings überhaupt
	// set ACTION key auf SAVE, player loop prüft (nach ready) den und speichert und fängt direkt wieder an ohne wait() zu gehen sodass dann direkt auch der screen gewechselt wird. 
}
